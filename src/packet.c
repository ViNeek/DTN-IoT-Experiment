#include "packet.h"

// Mule data cache
static struct iotCachedPacket	g_DataCache[CACHE_COUNT];

int iot_cache_init(struct iotPacket* p) {
	
}

int iot_cache_add(struct iotRecvPacket* p) {
	
}

struct iotCachedPacket* 	iot_cache_find(struct iotPacket* p) {
	
}